pipeline {
  agent any
  stages {
    stage('build') {
      steps { 
        dir('app') {
                    sh ''' sudo /usr/local/bin/docker-compose up --build --detach'''
                }
      }
    }
    stage('test') {
      steps {
        dir('app') {
          sh 'python3 app_test.py'
        }
      }
      post {
        always {
          junit 'test-reports/*.xml'
        }
      }
    }
  }
}