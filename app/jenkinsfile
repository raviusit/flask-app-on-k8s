pipeline {
  agent any
  stages {
    stage('build') {
      steps {
        sh ''' cd app/ '''
        sh ''' echo pwd '''
        sh ''' /usr/local/bin/docker-compose up --build --detach'''
      }
    }
    stage('test') {
      steps {
        sh 'python3 app_test.py'
      }
      post {
        always {
          junit 'test-reports/*.xml'
        }
      }
    }
  }
}